import 'package:flutter/material.dart';
import '../home.dart';

class HomeWithProductPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Home(initialIndex: 1);
  }
}
